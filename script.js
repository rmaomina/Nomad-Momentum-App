function init () {
  // Clock Init
  // getTime();
  // setInterval(getTime, 1000);

  // Greeing Init
  // loadName();

  // todo Init
  // loadTodos();
  // todoForm.addEventListener("submit", handleSubmit);
}

init();